using Util.Aoc;

int Part1(string data)
{
  return 0;
}

int Part2(string data)
{
  return 0;
}

string Reader(string file)
{
  return File.ReadAllText(file);
}

Solver.RunExamples({{ year }}, "{{ day }}", Reader, Part1, Part2);
Solver.RunSolution({{ year }}, "{{ day }}", Reader, Part1, Part2);